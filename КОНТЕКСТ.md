# Тілашар — контекст проекта

**Обновлено:** 3 сентября 2026
**Версия приложения:** 10.0
**Предыдущий документ:** `PROJECT_CONTEXT.md` описывает версию 5.0 и местами устарел — при расхождениях верен этот файл.
**Ход работы и обоснования решений:** указатель всех сессий — `СЕССИИ.md`.

---

## 0. Главное на сегодня: проект упёрся в документы

**Приложение готово, а на олимпиаду его пока не берут.** 2 сентября учитель
сообщила, что защищать будут старую, прошлогоднюю версию — Java-приложение
под Windows, с которым победили на школьном этапе. Причина названа прямо:
на новую программу надо заново писать документацию, а времени на это нет.

**Возражений было три. На 3 сентября закрыты все три:**

| № | Что сказано | Состояние |
|---|---|---|
| 1 | «Надо заново делать документацию, не успею» | Документы написаны, `документы/` |
| 2 | «Абсолютно другие задания, нужна только грамматика» | «Практика» объяснена, грамматическое ядро выведено вперёд |
| 3 | «Порядок должен быть диалог, правила, задания» | Показан на первом экране и полосой внутри урока |

Отдельно прозвучало исходное задание, которого раньше никто не передавал:
«шрифт больше, красочный, ярче, и персонаж, учитель, двигался, либо
говорил — оживить». Оно тоже выполнено: приложение озвучено целиком
(623 записи казахскими голосами), персонаж дышит, покачивается в такт
речи, реагирует на ответ.

Замеры — `АУДИТ-03-09.md`. Ход работы — `СЕССИЯ-3-сентября.md`.

**Основание считать новую версию тем же проектом уже есть — в её же
документации.** Раздел 11.5 технического описания прошлогодней защиты,
«Мобильді нұсқа», перечисляет среди планов развития «PWA нұсқасы –
веб-браузерде» и «Android нұсқасы». Сделано ровно это. Структура урока
диалог → правило → задание сохранена один в один: в `LessonScreen.tsx` фазы
идут `dialogue → grammar → task → verdict → summary`.

**Документы написаны 2 сентября.** Оба собраны по её же оглавлению, раздел
в раздел: техническое описание (12 разделов, 15 страниц) и руководство
пользователя (10 разделов, 16 страниц, 11 скриншотов). Лежат в `документы/`,
там же тексты и сборщик. Учителю остаётся прочитать и поправить казахский,
а не писать с нуля.

Разбор целиком, расшифровка голосового и три варианта выхода —
`олимпиада/ОТ-УЧИТЕЛЯ-02-09.md`. **Папка `олимпиада/` мимо git:** там чужие
документы и запись частного разговора, а репозиторий публичный.

Решение учителя пока прежнее: защищать прошлогоднее приложение. Но все
названные причины сняты, и следующий шаг — показать ей результат.

**Ближайшие шаги — `ПЛАН-НА-4-СЕНТЯБРЯ.md`.**

---

## 1. Что это

**«Тілашар»** — «тіл» язык, «ашар» откроет; так называли первые
словари-разговорники. Название объяснено на первом экране приложения и на
титуле презентации: имя, которое надо гуглить, работает против продукта.

Тренажёр казахского языка для русскоязычных школьников 12–17 лет. Веб-приложение, работает офлайн, ставится на телефон.

Проект готовится к защите на олимпиаде научных проектов (Дарын / Зерде / НИШ). Автор-докладчик — школьник; ему нужно не только показать продукт, но и объяснить, как он устроен.

**Где лежит:** `/Users/semenkuzelnyj/Documents/Default Project/grammarquest-web/`

---

## 2. Адреса

| Что | Адрес | Состояние |
|---|---|---|
| Боевой сайт | https://tilashar-kz.vercel.app | основной адрес, под именем приложения |
| Прежний адрес | https://grammarquest-web.vercel.app | продолжает работать, ведёт на ту же сборку |
| APK | https://github.com/s-chaikovskiy/grammarquest-web/releases | последний выпуск `v1.1`, 4,9 МБ, скачивается без входа |
| Репозиторий | https://github.com/s-chaikovskiy/grammarquest-web | публичный, ветка `main` |
| Сборка APK | https://github.com/s-chaikovskiy/grammarquest-web/actions | см. раздел 9 |
| Vercel-проект | `chaikovskiy-s-projects/grammarquest-web` | автодеплой с `main` |

Доступ с этой машины: GitHub по SSH (`s-chaikovskiy`), Vercel CLI установлен и авторизован.

**Важно про preview-адреса Vercel:** в аккаунте включена Deployment Protection, поэтому preview-ссылки открываются только тому, кто залогинен в Vercel. Для показа посторонним годится только боевой адрес.

---

## 3. Что было не так в версии 5.0

Всё перечисленное — проверенные факты, а не предположения.

### Проверка ответов не работала для казахского

166 из 187 правильных ответов содержат буквы `ә ғ қ ң ө ұ ү һ і`. На русской раскладке их физически негде взять, а проверка была строгим сравнением строк. Школьник, ответивший грамматически верно, получал «неправильно».

### Пять типов заданий существовали только на бумаге

Ни у одного из 187 шагов в `lessons.json` не было поля `taskType`. Работал ровно один тип — ввод текста. Остальные четыре были мёртвым кодом.

### Задание на сопоставление уронило бы приложение

В `TaskInput.tsx` `useState` вызывался внутри условной функции рендера. Нарушение правил хуков React: при переключении типа задания порядок хуков ломается и экран падает.

### Шрифты не содержали казахских букв

Проверено разбором cmap скачанных файлов:

- **Manrope** (основной) — нет `ә ғ қ ң ұ`
- **Playfair Display** (заголовки) — нет 7 из 9

Эти буквы молча подменялись системным шрифтом — в приложении для изучения казахского ключевые символы рендерились чужой гарнитурой.

### Шрифты грузились с Google CDN

Приложение заявлено как офлайн-PWA, но шрифт приезжал из сети. Без интернета — подмена.

### Изображения персонажей весили 11 МБ

Семь PNG по 1024×1024 (~1,6 МБ каждый) при отображении в 44–96 px. Вокруг фигур запечена полупрозрачная серая дымка — персонаж выглядел вставленным в грязный прямоугольник.

### Интервального повторения не было

Экран «Повторение» существовал, алгоритма не было — он просто прогонял пройденное подряд.

### Переключатель языка почти не работал

Проверка ответа всегда шла по `step.answerKz`, задание всегда показывалось по-казахски.

### Дизайн противоречил собственной документации

`STATUS-v5.md` перечисляет glassmorphism и градиенты как «AI slop, чего избегать». `PROJECT_CONTEXT.md` хвалит их как достижение. В коде были и glassmorphism, и mesh-градиенты, и частицы, и 3D-наклон карточек.

### Содержание уроков было двусмысленным

Казахская ветка учит казахской грамматике, русская — русской, но подписаны они как «перевод». Согласовано: продукт учит **казахскому для русскоязычных**, русский — только подсказка.

---

## 4. Что сделано

### 4.1 Механика

**Движок проверки ответов** — `src/utils/answer.ts`

Многослойная проверка вместо сравнения строк:

1. Точное совпадение
2. Совпадение после свёртки казахских букв к русским (снимает проблему раскладки) → ответ засчитывается, показывается правильное написание
3. Расстояние Левенштейна для опечаток, бюджет зависит от длины ответа
4. Варианты через `/`, пояснения в скобках

Вердикты: `correct` / `correct_kz` / `almost` / `wrong`. Короткие ответы опечатки не прощают — иначе «тұр» и «тұт» слились бы.

**Экранная клавиатура казахских букв** — ряд `ә ғ қ ң ө ұ ү һ і` под каждым полем ввода.

**Интервальное повторение** — `src/utils/srs.ts`

Упрощённый SM-2. Каждый шаг урока — карточка. Интервалы 1 → 3 → дальше по коэффициенту лёгкости, потолок 180 дней. Ошибка возвращает карточку на сегодня. В очередь первыми идут те, что забывались чаще.

**Метрики и выгрузка** — `src/utils/metrics.ts`

Журнал ответов: тип задания, вердикт, время на ответ, подсказки. Выгрузка CSV с разделителем `;` и BOM — Excel на Windows открывает кириллицу без плясок. Личные данные не собираются, только метка участника.

### 4.2 Контент

**Генератор заданий** — `tools/enrich_lessons.py`

Сборка контента идёт в два шага (`npm run data`):

1. `tools/content/build.py` объединяет исходные 18 уроков и базовый курс из
   `tools/content/beginner.py`, проставляет уровни и выгружает новое на проверку
2. `tools/enrich_lessons.py` раскладывает 225 шагов по семи типам и собирает словарь

| Тип | Сколько |
|---|---|
| `fill_blank` — пропуск в предложении | 63 |
| `choice` — выбор из 4 вариантов | 56 |
| `open` — развёрнутый ответ с самопроверкой | 23 |
| `input` — свободный ввод | 22 |
| `word_order` — сборка предложения | 22 |
| `matching` — сопоставление формы и перевода | 21 |
| `translate` — перевод на казахский | 18 |

На первом уровне сборка предложений не даётся: ученик ещё не знает порядка
слов, поэтому вместо неё ставится пропуск.

**Дистракторы для выбора** строятся по правилам казахской морфологии: смена лица с сохранением сингармонизма (`ішемін / ішесің / ішеміз / ішесіз`), смена времени, другие формы того же глагола для деепричастий на `-ып/-іп`.

Кандидаты с невозможными стыками (`гласная+гласная`, `й+гласная`) отбраковываются — лучше простой дистрактор, чем выдуманная форма, на которой ученик запомнит неправильное.

**Правка контента.** Исходные уроки — `src/data/lessons.source.json`, базовый
курс — `tools/content/beginner.py`. После правки обязательно `npm run data`.
Файлы `lessons.json`, `lessons.merged.json` и `vocabulary.json` генерируются —
править их напрямую бесполезно.

Для служебных слов дистракторы задаются вручную в `DISTRACTORS` внутри
`beginner.py`: у них нет окончаний, и правило подставляет случайные слова урока.

### 4.3 Дизайн

Убрано: glassmorphism, mesh-градиенты, частицы, 3D-наклон, градиентные кнопки со свечением, эмодзи вместо иконок, заглавные «надзаголовки» над каждым блоком.

**Шрифт** — Golos Text, вшит в бандл (4 сабсета, 96 КБ). Полное покрытие казахских букв проверено разбором cmap. Спроектирован под кириллические интерфейсы.

**Палитра** — OKLCH, светлая тема по умолчанию, полноценная тёмная, переключатель «как в системе / светлая / тёмная».

Контраст проверяется скриптом `tools/contrast.py` — 36 проверок WCAG AA, все проходят. **Палитру править только вместе с прогоном скрипта.**

**Типографика** — фиксированная шкала кегля (шаг ≈1,2), без текучего `clamp`: в интерфейсе задачи заголовок не должен съёживаться в узкой колонке.

**Движение** — 140–280 мс, экспоненциальный ease-out, без отскока. Полная поддержка `prefers-reduced-motion`.

**Правило, которое легко нарушить снова:** видимость содержимого не должна зависеть от анимации. `initial={{ opacity: 0 }}` на замороженной вкладке не доигрывает, и экран остаётся пустым. Анимируется только сдвиг.

**Изображения** — `tools/optimize_characters.py`: гасит дымку, обрезает по фигуре, уменьшает до 320 px, сохраняет WebP. **11,1 МБ → 165 КБ.** Исходные PNG лежат в `assets-source/characters/`.

### 4.4 Экраны

С нижней панелью: `/learn` путь обучения · `/practice` практика ·
`/dictionary` словарь · `/reference` справочник (+ `/rules`, `/topics`,
`/tables`) · `/stats` профиль

На весь экран, без панели: `/` приветствие · `/lesson/:id` урок ·
`/review` повторение · `/cards` карточки · `/sprint` спринт

Роутер — `HashRouter`, не `BrowserRouter`: в APK приложение загружается не с обычного адреса, и history API там ломается на переходах.

### 4.5 Мобильное приложение

- **PWA** — работает сейчас, ставится из браузера на Android и iPhone
- **APK** — Capacitor 8, нативный проект в `android/`, сборка в GitHub Actions (локально Android SDK не нужен — на этой машине его и нет)

### 4.6 Тесты

70 тестов, без внешних зависимостей — собираются esbuild-ом, бегут в node:

- `tools/tests/answer.test.ts` — 27, движок проверки ответов и задания с пропуском
- `tools/tests/srs.test.ts` — 15, интервальное повторение и метрики
- `tools/tests/morphology.test.ts` — 21, склонение и спряжение
- `tools/tests/helpers.test.ts` — 7, говорящий в реплике и склонение числительных

Запуск: `npm test`. Гоняются и в облачной сборке перед APK.

### 4.7 Презентация

`презентация/` — 8 слайдов, PPTX + PDF, движок `deckkit.py`, заметки докладчика внутри файла. Подробности — `презентация/ЧИТАТЬ-ПЕРВЫМ.md`.

**Незаполненное:** ФИО школьника, класс, школа, город, научный руководитель — прочерки в шапке `build.py`.

---

## 4а. Что изменилось в версии 7

### Выбор языка убран

Приложение с самого начала сделано для тех, кто говорит по-русски и учит
казахский, — выбирать было нечего, а первый экран начинался с непонятного
вопроса. Теперь интерфейс русский, казахский живёт в содержании уроков.
Вместо выбора языка на первом экране — выбор уровня, который действительно
меняет то, с чего начнётся обучение.

### Три уровня вместо общего списка

| Уровень | Класс | Уроков | О чём |
|---|---|---|---|
| Начало | 5–7 | 12 | Первые слова, счёт, семья, школа, база грамматики |
| Уверенный | 7–9 | 7 | Септіктер и времена глагола |
| Свободный | 9–11 | 7 | Наклонения, залоги, сложная речь |

Уровень — ориентир, а не ограничение: переключается в одно касание, уроки
жёстко не запираются.

### Базовый курс для 5–7 классов

Курс начинался сразу с грамматики уровня 8–11 класса — пятикласснику не за
что было зацепиться. Добавлены восемь тематических уроков: знакомство, счёт,
семья, школа, цвета, еда, распорядок дня, погода. Лексика идёт впереди
грамматики, грамматика вводится небольшими порциями.

**Эти уроки писал ассистент, а не носитель языка.** Они помечены
`needsReview` и целиком выгружены в `НА-ПРОВЕРКУ-УЧИТЕЛЮ.md` — до проверки
живым учителем считать их готовыми нельзя.

### Пять новых разделов

- **Словарь** — 162 слова, собранных из самих уроков, поиск работает с
  русской раскладки, знакомые слова помечены
- **Карточки** — быстрая лексика по тому же алгоритму повторения
- **Спринт** — 60 секунд на скорость, ошибка отнимает три секунды, рекорд
  хранится на устройстве
- **Таблицы окончаний** — подставь своё слово, увидишь все формы с
  подсвеченным окончанием и объяснением, почему выбрано именно оно
- **Профиль** — статистика, настройки, выгрузка CSV

Навигация переехала на нижнюю панель: раньше до словаря или статистики нужно
было возвращаться через несколько экранов.

### Что взято из Duolingo

- **Карта-путь** вместо списка уроков: видно пройденное и следующий шаг
- **Цель на день** и календарь недели
- **Разбор ошибок** в конце урока с повторным проходом (без начисления XP —
  он нужен, чтобы разобраться, а не набрать очки)
- **Мгновенная реакция** на выбор варианта, без кнопки «Проверить»

### Морфология

Отдельный модуль `src/utils/morphology.ts` с правилами сингармонизма,
озвончения и выбора окончания по последнему звуку. Покрыт 21 тестом на
учебниковых формах. Именно на нём работают таблицы окончаний.

Тонкие места, которые уже ловились тестами:

- «кітап» + «ым» даёт **«кітабым»**: конечные қ, к, п озвончаются перед
  гласным окончанием
- «оқы» + личное окончание даёт **«оқимын»**, и ряд гармонии берётся от
  исходной основы — по букве «и» его восстановить нельзя
- множественное число: после р, й, у ставится -лар/-лер, а после л, м, н, ң —
  -дар/-дер; общего правила «все сонорные одинаково» нет

### Анимации

Движение добавлено туда, где оно несёт смысл: верный ответ подпрыгивает,
неверный вздрагивает, счётчик XP пробегает значения, галочка на пройденном
узле прорисовывается, варианты появляются по очереди, персонаж меняет позу
по результату. Всё отключается по `prefers-reduced-motion`.

Библиотека анимаций убрана: она осталась ради простых сдвигов, которые
переехали в CSS.

### Озвучка — система готова, аудио нет

Проверено: встроенная в браузер озвучка для казахского не работает. На типовой
машине 157 голосов и **ни одного казахского**. Поэтому браузерный синтез не
используется вовсе — иначе казахский текст читается чужим языком.

Что сделано:

- отобраны **372 фразы** (15 512 символов): слова, ответы, реплики диалогов;
  *в версии 8 манифест пересобран построчно — стало 589 фраз и 20 091 символ;*
  грамматические объяснения намеренно не озвучиваются — на слух они не
  воспринимаются
- `tools/tts/synthesize.py` — синтез через Azure (`kk-KZ-AigulNeural`,
  `kk-KZ-DauletNeural`), ключ берётся из переменных окружения
- `tools/tts/фразы-для-записи.md` — список для записи живым голосом
- кнопка «послушать» показывается только там, где запись есть

**Для защиты запись живого носителя сильнее платного синтеза.**

### Размер сборки

935 → 761 КБ (gzip 241 → 190 КБ). *Актуальные числа — в разделе 9.* Убрана библиотека анимаций, манифест
озвучки разделён: в браузер уходит только пара «код-текст».

---

## 4б. Что изменилось в версии 8

### Название

Приложение называется «Тілашар». Переименованы заголовок страницы, манифест
PWA, имя на домашнем экране Android, файл APK и презентация. **Ключи
сохранений `grammarquest_state` и `grammarquest_theme` намеренно оставлены
прежними** — переименование стёрло бы прогресс у всех, кто уже занимался.
Адрес сайта тоже прежний: менять работающую ссылку перед защитой незачем.

### Единая шапка экрана

`src/components/ScreenHeader.tsx` — одна шапка на всё приложение. У кнопки
возврата всегда есть подпись с названием места назначения: голая стрелка «←»
не говорит, куда ведёт, и ученик нажимает её вслепую. Там, где возврат ведёт
не на путь обучения, рядом стоит кнопка «домой».

Уход из начатого урока спрашивает подтверждение — но только если есть что
терять: до первого ответа и на итоговом экране вопрос не задаётся.

### Экран «Как устроено приложение»

`/help` — разделы, ход урока, правила проверки ответа, XP и повторение,
установка на телефон. Открывается с первого экрана, из профиля и из справки.

Раздел «Правила» переименован в «Справку»: внутри него лежал подраздел
с тем же названием, и два разных экрана назывались одинаково.

### Диалог показывается репликами

В 187 шагах из 225 «диалог» — это обмен репликами двоих, записанный в одно
поле через перевод строки. Экран показывал весь обмен под портретом первого
говорящего: выходило, что человек отвечает сам себе. Теперь каждая реплика
идёт со своим портретом и своим именем — `splitDialogue` в `helpers.ts`.

### Озвучка: манифест построчно и голос на каждого

Манифест пересобран так же построчно, иначе кнопка «послушать» рядом с
репликой не нашла бы файл. У каждой записи свой голос: женский читает слова,
ответы и Айшу, мужской — Диму и учителя, иначе двух собеседников в диалоге
не различить на слух.

**589 фраз, 20 091 символ** — весь курс укладывается в бесплатный объём
Azure (500 000 символов в месяц).

### Исправленные ошибки

- **Экран урока падал в белое.** `useCountUp` вызывался после условного
  возврата: переход с несуществующего урока на существующий менял число
  хуков между рендерами.
- **XP не начислялся на скрытой вкладке.** Счётчик выводился через
  `requestAnimationFrame`, а он на невидимой странице не срабатывает:
  ученик отвечал верно, число оставалось нулевым.
- **Анимации появления снова гасили содержимое через `opacity`.** Галочка
  пройденного урока и варианты ответа оставались невидимыми, если анимация
  не доигрывала. То же правило уже нарушалось раньше.
- **Кнопка «послушать» показывалась без записи.** И dev-сервер, и хостинг
  отвечают на несуществующий mp3 страницей приложения со статусом 200.
  Теперь проверяется тип содержимого.
- **Двойные отступы** на трёх экранах: профиль, правила и справочник
  заворачивались в `page`/`shell` поверх того, что даёт каркас.
- **`synthesize.py` читал манифест по несуществующему пути** — синтез не
  запустился бы вовсе.
- **Цели для пальца меньше 44 px:** фильтры-«таблетки», названия уроков
  на дорожке, кнопка «к уроку» в словаре.

### Презентация

Переписана от первого лица и разложена на две части: `тексты.py` —
все строки обеих версий, `build.py` — вёрстка, одна на оба языка.
Казахская версия собирается той же командой и **ждёт проверки учителем**:
`презентация/НА-ПРОВЕРКУ-КАЗАХСКИЙ-ТЕКСТ.md`, 76 строк.

---

## 4в. Что изменилось в версии 9

### Презентация переписана от лица ученика

Колода прошла путь 18 → 10 → 8 слайдов. Каждое сокращение делалось не ради
краткости, а потому что доклад отвечал не на тот вопрос: он был построен вокруг
приложения, а жюри спрашивает про автора.

| № | Слайд | О чём |
|---|---|---|
| 01 | Титул | Название, автор, четыре числа |
| 02 | Как это появилось | Краткий курс по ИИ → приложение, а не реферат |
| 03 | Зачем это нужно | Актуальность казахского и польза приложения |
| 04 | Что создано | Пять разделов, интерфейс, три уровня 5–11 класс |
| 05 | Как создано | Какими сервисами и что проверяется само |
| 06 | Что особенного | Четыре вещи, которых нет в обычном тренажёре |
| 07 | Попробуйте сами | QR-код, установка на телефон |
| 08 | Куда это может вырасти | Что уже сделано, план развития, просьба о поддержке |

Три вещи, которые ушли осознанно: разбор недостатков прежней версии (прототип
победил, и рассказывать о нём как о неудаче — обесценивать результат), три
слайда про казахские буквы вместо одного пункта из четырёх, и устройство кода
на слайде «как создано» — вместо него список сервисов.

Финал перестал быть «выводами» и стал просьбой: дать классы для апробации
и учителей-методистов.

### Офлайн: закрыт разрыв при первом заходе

Кэш `pages` наполняется только после того, как service worker встал на пост.
При самом первом заходе страница приходит по сети раньше, и в кэш попадает лишь
со следующей загрузки. Потеря сети внутри этого промежутка означала страницу
ошибки браузера. Добавлен `handlerDidError`, отдающий сохранённый `index.html`,
когда и сеть, и кэш `pages` уже не ответили. `navigateFallback` для этого не
годится: он вернул бы старую страницу всегда.

Проверено по факту: APK содержит весь сайт внутри пакета; в браузере после
одного захода 33 файла в кэше и ноль запросов в сеть на шести экранах.
**На iPhone обязательно «На экран „Домой“»**: обычная вкладка Safari попадает
под правило iOS, которое чистит хранилище сайта примерно через неделю без
открытия — вместе с прогрессом ученика.

### Три новые автопроверки

- `сверка-цифр.py` считает теперь и автотесты, и проверки контраста: 18 значений
  вместо 14. Поводом стало «28 проверок контраста» на слайде при фактических 36.
- `проверка-переносов.py` ищет пункты списков, которые после переноса начинаются
  с тире и потому читаются как лишний пункт.
- Сборка колоды проверяет нижнее поле листа: проверка карточек меряла только их
  высоту, а не то, куда их поставили, и казахская версия уезжала за край молча.

---

## 5. Ключевые файлы

```
src/utils/answer.ts           движок проверки ответов
src/utils/morphology.ts       склонение и спряжение (таблицы окончаний)
src/utils/srs.ts              интервальное повторение
src/utils/metrics.ts          статистика и выгрузка CSV
src/utils/speech.ts           поиск и проигрывание записей
src/components/TaskInput.tsx  семь типов заданий, у каждого свои хуки
src/components/AppShell.tsx   каркас с нижней панелью
src/components/ScreenHeader.tsx шапка экрана: возврат с подписью и «домой»
src/hooks/useAppState.ts      состояние, миграция сохранений
src/index.css                 дизайн-система целиком

src/screens/LearnScreen.tsx      путь обучения — главный экран
src/screens/PracticeScreen.tsx   цель на день, повторение, карточки, спринт
src/screens/DictionaryScreen.tsx словарь
src/screens/TablesScreen.tsx     живые таблицы окончаний
src/screens/SprintScreen.tsx     игра на 60 секунд
src/screens/CardsScreen.tsx      карточки слов
src/screens/HelpScreen.tsx       как устроено приложение

src/data/lessons.source.json     исходные 18 уроков — править здесь
tools/content/beginner.py        базовый курс 5–7 класса — править здесь
src/data/lessons.merged.json     генерируется
src/data/lessons.json            генерируется
src/data/vocabulary.json         генерируется
src/data/audio-index.json        генерируется

tools/content/build.py           сборка контента и выгрузка на проверку
tools/enrich_lessons.py          генератор типов заданий и словаря
tools/contrast.py                проверка контраста палитры
tools/export_reference_review.py выгрузка правил и справочника учителю
tools/check_fonts.py             покрытие казахских букв шрифтом (слайд 07)
tools/check_class_names.mjs      столкновения своих классов с утилитами Tailwind
tools/md_to_pdf.py               любой .md проекта в PDF для печати и раздачи
tools/optimize_characters.py     подготовка изображений
tools/tts/build_manifest.py      список фраз для озвучки
tools/tts/synthesize.py          синтез через Azure
tools/tests/                     автотесты

.github/workflows/android.yml    облачная сборка APK
android/                         нативный проект (в репозитории намеренно)
презентация/тексты.py            все тексты колоды, русский и казахский
презентация/build.py             вёрстка колоды, одна на оба языка
презентация/сверка-цифр.py       сверка 18 чисел колоды с данными проекта
презентация/проверка-переносов.py  тире в начале перенесённой строки списка
презентация/выгрузка-на-проверку.py  казахский текст на сверку учителю
НА-ПРОВЕРКУ-УЧИТЕЛЮ.md           весь казахский текст уроков на сверку
НА-ПРОВЕРКУ-СПРАВОЧНИК.md        правила и темы справочника на сверку
```

---

## 6. Команды

```bash
npm run dev       # разработка, порт 5173
npm test          # 70 тестов
npm run data      # пересобрать контент: уровни, задания, словарь, список на проверку
npm run build     # продакшн-сборка
```

```bash
python3 tools/contrast.py          # проверка палитры, обязательно после правки цветов
python3 tools/contrast.py --hex    # то же плюс hex-значения
```

```bash
tools/.venv/bin/python tools/check_fonts.py --скачать   # казахские буквы в шрифтах
```

```bash
cd презентация && ./venv/bin/python сверка-цифр.py         # 18 чисел колоды против данных
cd презентация && ./venv/bin/python проверка-переносов.py  # тире в начале перенесённой строки
```

```bash
презентация/venv/bin/python tools/md_to_pdf.py ФАЙЛ.md  # документ в PDF
```

```bash
python3 tools/tts/build_manifest.py   # пересобрать список фраз для озвучки
python3 tools/tts/synthesize.py       # синтез через Azure (нужен ключ в окружении)
```

```bash
git push origin main               # деплой прода + сборка APK
vercel deploy --yes                # preview, прод не трогает
```

Пересборка презентации:

```bash
cd презентация && ./venv/bin/python build.py
```

---

## 7. Согласованные решения

| Вопрос | Решение |
|---|---|
| Чему учит | Казахскому для русскоязычных; русский — подсказка |
| Мобильное приложение | APK через Capacitor + доработанный PWA |
| Где собирать APK | GitHub Actions, локально SDK не ставим |
| Тип олимпиады | Научный проект (Дарын / Зерде / НИШ) |
| Формат презентации | PPTX + PDF |
| Апробация | Не проводилась; стоит первым пунктом в плане развития на слайде 08 |

**Про апробацию отдельно.** Выдуманных процентов в презентации нет. В приложение встроен сбор метрик и выгрузка CSV — если прогнать один класс за 5 учебных дней, появятся настоящие цифры, и слайд станет сильнейшим в колоде. До защиты больше месяца, времени хватает.

---

## 8. Чего не повторять

- **Не править `lessons.json` руками** — правки затрёт `npm run data`.
- **Не называть свои классы словами из Tailwind** — утилита подмешается
  молча, и правило появится из ниоткуда. Так `.ring` получил контур цветом
  текста: чёрный прямоугольник в светлой теме, белый в тёмной, при пустых
  `border` и `outline` в вычисленных стилях. Проверка: `npm run check:css`.
- **Не менять палитру без `tools/contrast.py`** — контраст рассчитан, а не подобран на глаз.
- **Не возвращать `initial={{ opacity: 0 }}`** на появление экранов — содержимое пропадает, если анимация не доиграла.
- **Не ставить `mode="wait"` у `AnimatePresence`** там, где от этого зависит показ следующего шага.
- **Не подключать шрифты с Google CDN** — приложение обязано работать офлайн.
- **Не игнорировать `android/app/src/main/assets/` целиком** — git не хранит пустые каталоги, и облачная сборка падает. В каталоге лежит `.gitkeep`, он нужен.
- **Не заменять шрифт, не проверив покрытие казахских букв** — большинство популярных гарнитур их не содержит.
- **Не расширять допуск на опечатки** — в казахском два символа это целое окончание, то есть другое лицо. Прощается только потерянная или лишняя буква, но не подменённая.
- **Не отдавать страницу из кэша первой** — иначе обновления не доходят до
  вернувшихся, и никакая правка внутри приложения это не лечит. Одного
  `navigateFallback: null` мало: нужен ещё `directoryIndex: null`, иначе
  precache перехватывает адрес «/» раньше правила NetworkFirst. Признак,
  что всё сошлось: на боевом адресе в `caches.keys()` появляется кэш `pages`.
- **Не спрашивать наличие файла по сети на каждом экране** — список
  доступных записей составляется при сборке содержания. Положили mp3
  в `public/audio` — прогоните `npm run data`, иначе приложение о них
  не узнает.
- **Не пересоздавать экран урока без ключа по адресу** — состояние прошлого
  урока остаётся, и разбор ошибок обращается к несуществующему шагу.
- **Не подключать встроенную в браузер озвучку для казахского** — казахских голосов в системе нет, вместо казахского получится текст, прочитанный чужим языком.
- **Не менять морфологические правила без прогона `npm test`** — там 21 тест на учебниковых формах, и правила легко сломать в тонком месте.
- **Не переименовывать ключи в localStorage** — `grammarquest_state` и
  `grammarquest_theme` остались от прежнего имени намеренно: переименование
  стирает прогресс у всех, кто уже занимался.
- **Не выводить число через `requestAnimationFrame`** — на скрытой вкладке
  кадры не приходят, и значение застревает. Анимация может украшать
  изменение, но не быть единственным способом его показать.
- **Не проверять наличие файла по одному статусу 200** — и dev-сервер,
  и хостинг отдают страницу приложения вместо ошибки.
- **Не вызывать хуки после условного возврата** — число хуков между
  рендерами меняется, и экран падает.
- **Не гасить текст прозрачностью** — контраст палитры рассчитан, а
  прозрачность накладывается поверх и роняет его незаметно. Приглушать
  цветом `--ink-2`, различать состояния формой (пунктир, рамка).
  Исключение — отключённые контролы: WCAG их не требует.
- **Не оставлять мёртвый код** — из-за него прежняя версия и разошлась
  с собственным описанием. Проверка: `npx tsc --noEmit --noUnusedLocals`.
- **Не считать базовый курс готовым до проверки учителем** — его писал ассистент, не носитель языка.

---

## 9. Текущий статус

**Готово и проверено:**

- 26 уроков, 225 заданий, три уровня (12 / 7 / 7); словарь на 162 слова
- Шесть разделов, нижняя панель, карта-путь, экран «как устроено»
- 70 тестов зелёные, типы сходятся, сборка проходит
- Палитра проходит 36 проверок WCAG AA
- Все экраны пройдены вживую: у каждого свой заголовок, возврат подписан,
  горизонтальной прокрутки нет нигде, целей мельче 44 px не осталось
- Код и стили в сжатом виде 202 КБ; вся сборка вместе со шрифтом
  и изображениями — 1,4 МБ
- **Офлайн проверен по факту:** после одного захода 33 файла в кэше, ноль
  запросов в сеть на шести экранах, ноль обращений к чужим доменам. APK
  содержит весь сайт внутри пакета и сервера не требует
- **Озвучка записана целиком:** 623 файла, казахские голоса Айгуль и Даулет.
  Реплики диалогов 412 из 412, слова словаря 162 из 162. Ключ Azure
  не понадобился — те же голоса доступны через edge-tts
- **Персонаж анимирован:** дыхание, покачивание в такт речи, реакция
  на ответ. Настройка «Уменьшить движение» гасит всё это разом
- **Порядок урока показан:** блок на первом экране и полоса внутри урока
- **APK собирается**: восемь последних облачных сборок подряд успешны, файл
  4,3 МБ. Причина прежних падений была одна — `@capacitor/cli` требует Node 22,
  а в workflow стоял Node 20. PWA ставится на телефон из браузера
- Презентация переписана под версию 9: **8 слайдов**, PPTX + PDF, русская
  и казахская версии из одной вёрстки, ФИО автора вписано. QR-код на слайде 07
  сверен попиксельно — ведёт на tilashar-kz.vercel.app. Заметки докладчика есть
  на всех восьми слайдах обеих версий
- **Релиз `v1.1` опубликован**: APK 4,9 МБ качается по прямой ссылке без
  входа в GitHub. В `v1.0` попали падение при переходе между уроками и
  лишние запросы в сеть — на защиту брать `v1.1`

**Не готово:**

- **Строка «уже сделано» на слайде 08** — «тексты проверил учитель казахского
  языка · приложение попробовали одноклассники». На момент записи это ещё не
  правда. Она обязана стать ею до защиты: первый же вопрос «кто именно проверял?»
  разрушает доверие ко всей колоде. ФИО учителя и число ребят вписываются
  в `презентация/тексты.py` — поля `TEACHER_RU`, `TEACHER_KZ`, `TESTERS`.

- **Титульные данные** вписаны: Малышко Макар Евгеньевич, 6 «б» класс,
  школа №35. Пустыми остались город и научный руководитель — если они нужны
  на титуле, заполняются там же, в верхнем блоке `тексты.py`.

- **НИ ОДНА СТРОКА КАЗАХСКОГО НЕ ПРОВЕРЕНА НОСИТЕЛЕМ ЯЗЫКА.** Это главное
  незакрытое место проекта, и оно больше, чем казалось раньше:

  | Где | Знаков казахского | Файл на сверку |
  |---|---|---|
  | Уроки (все 26, не только новые 8) | 93 905 | `НА-ПРОВЕРКУ-УЧИТЕЛЮ.md` |
  | Правила и справочник | 73 785 | `НА-ПРОВЕРКУ-СПРАВОЧНИК.md` |
  | Текст презентации | — | `презентация/НА-ПРОВЕРКУ-КАЗАХСКИЙ-ТЕКСТ.md` |
  | **Всего** | **167 690** | |

  Прежняя редакция этого документа выделяла только 8 новых уроков и тем
  создавала ложное впечатление, будто остальное проверено. Исходные
  18 уроков сгенерировала прежняя нейросеть, ассистент внёс 44 правки —
  носитель языка не видел ни одной строки.

- **Озвучки нет** — конвейер готов и проверен, нужен ключ Azure в переменной
  окружения либо записи живого голоса. 589 фраз, 20 091 символ, список
  в `tools/tts/фразы-для-записи.md`. Запуск: `python3 tools/tts/synthesize.py`.

- **Персонажи прежние, и происхождение картинок неизвестно.** Файлы
  достались от версии, написанной другой нейросетью; чем сгенерированы
  или откуда взяты — не задокументировано. Жюри вправе спросить.
  Подробности и варианты решения — `assets-source/ОТКУДА-КАРТИНКИ.md`.
  Присланная картинка с новыми героями так и не сохранена.

- **Лицензии у репозитория нет.** Репозиторий публичный, а файла `LICENSE`
  в корне нет — формально это «все права защищены». Для школьного проекта,
  который показывают как открытый, стоит выбрать лицензию (обычно MIT).
  Решение за владельцем, поэтому файл не добавлен самовольно.

- **Апробация** — инструмент готов, данных нет.

---

## 10. Что дальше

1. **Решить вопрос с документами** — см. раздел 0. Без этого остальное
   не имеет смысла: приложение, которое не берут на олимпиаду, не нужно
   ни озвучивать, ни апробировать.
2. **Проверка учителем.** Базовый курс — `НА-ПРОВЕРКУ-УЧИТЕЛЮ.md`,
   казахский текст колоды — `презентация/НА-ПРОВЕРКУ-КАЗАХСКИЙ-ТЕКСТ.md`
   (76 строк, в шапке — четыре места, в которых сомневается сам переводчик).
   Это блокирует остальное: править презентацию под содержание, которое
   может измениться, значит делать её дважды.
3. **Раздать ссылку одноклассникам** и вписать их число в `TESTERS`, ФИО
   учителя — в `TEACHER_RU` / `TEACHER_KZ`. После этого пересобрать колоду.
4. **Проверить на настоящем телефоне.** Ни APK, ни PWA ни разу не запускались
   на живом устройстве. Порядок: открыть сайт, добавить на экран «Домой»,
   позаниматься минуту, включить авиарежим, открыть снова.
5. **Озвучка.** Ключ Azure в переменную окружения — и `synthesize.py`
   записывает 589 файлов за один заход.
6. Прогнать апробацию на классе и подставить настоящие цифры вместо плана.

---

## 11. История версий

- **1.0** — десктопное приложение на Java (JavaFX + Maven)
- **2.0–4.0** — переписано на React, эксперименты с оформлением
- **5.0** — тёмная тема, glassmorphism, геймификация
- **6.0** — переработаны механика, дизайн, контент и мобильная сборка; добавлены тесты, интервальное повторение, статистика и презентация
- **7.0** — убран выбор языка, три уровня, базовый курс 5–7 класса, пять новых разделов, анимации, система озвучки
- **8.0** — имя «Тілашар», единая шапка с подписанным возвратом, экран «как устроено», диалоги по репликам, презентация на двух языках
- **9.0** — презентация переписана от лица ученика и сведена к 8 слайдам, закрыт разрыв в офлайн-работе при первом заходе, добавлены проверки чисел доклада, переносов в списках и нижнего поля слайда
- **10.0** — озвучка (623 записи), анимированный персонаж, показанный порядок урока, работающая настройка «Уменьшить движение»; исправлены задание с ответом-цифрой, кэш озвучки и зона нажатия кнопки «послушать»
