# -*- coding: utf-8 -*-
"""
GrammarQuest — защита научного проекта. Сборка колоды.

Пересборка после правки текста:
    ./venv/bin/python build.py
    soffice --headless --convert-to pdf GrammarQuest-защита.pptx
"""
from deckkit import *

# ─────────────────────────────────────────────────────────────────────
# ДАННЫЕ АВТОРА. Заполнить перед защитой — они попадают на титул и в финал.
AUTHOR = "Фамилия Имя"
GRADE = "__ класс"
SCHOOL = "школа №__, г. ____"
MENTOR = "научный руководитель: ____"
CITY_YEAR = "Казахстан · 2026"
SITE = "grammarquest-web.vercel.app"
# ─────────────────────────────────────────────────────────────────────

d = Deck()
d.total = 18
N = [0]


def sl(eyebrow, dark=False):
    N[0] += 1
    s = d.add(dark=dark)
    y = chrome(s, d, eyebrow, N[0], dark=dark)
    return s, y


def notes(slide, text):
    """Заметки докладчика: видны выступающему, но не залу."""
    slide.notes_slide.notes_text_frame.text = text.strip()


# ═══════════════════════════════════════════════════ 01 · титул
N[0] += 1
s = d.add()
title_slide(
    s, f"научный проект · {CITY_YEAR}",
    "Почему тренажёры", "казахского не работают",
    "Исследование причин и разработка приложения GrammarQuest — тренажёра "
    "казахской грамматики для русскоязычных школьников.",
    [("18", "уроков"), ("187", "заданий"), ("7", "типов упражнений"), ("90%", "ответов требуют особых букв")],
    f"{AUTHOR} · {GRADE} · {SCHOOL} · {MENTOR}")

# Данные автора включены в подзаголовок одной строкой: заголовочный макет
# переносит текст только по ширине, и отдельные абзацы в нём наезжали на цифры.
notes(s, """
Здравствуйте. Меня зовут [имя], я учусь в [класс].

Мой проект начался с личного наблюдения: я пользовался приложениями для изучения
казахского и всё время получал «неверно» там, где ответ был правильным.
Я решил разобраться, почему так происходит, — и оказалось, что причина
техническая, и её можно устранить.

Держать 8–9 минут. На титуле не задерживаться дольше 30 секунд.
""")

# ═══════════════════════════════════════════════════ 02 · проблема
s, y = sl("актуальность")
y = title(s, "Казахскому учат, но учить его на компьютере почти нечем", y, max_lines=2, start=38)
y = lede(s, "Казахский язык изучают во всех школах Казахстана, в том числе там, где обучение идёт на русском. "
            "При этом цифровых тренажёров, рассчитанных именно на русскоязычного ученика, крайне мало.", y + 0.16, w=CW * 0.88)
yy = y + 0.46
hline(s, ML, yy - 0.22, CW)
lx, lw = col(0, 6)
rx, rw = col(6, 6)
b1 = block(s, wrap("На duolingo.com в разделе курсов для говорящих по-русски доступно девять языков: "
                   "английский, немецкий, французский, испанский, китайский, корейский, японский, "
                   "итальянский, португальский. Казахского в этом списке нет.", TEXT_SB, 13, lw - 0.56),
           lx + 0.28, yy + 0.30, lw - 0.56, TEXT_SB, 13, INK, leading=19)
panel_behind(s, lx, yy, lw, bottom=b1, pad=0.30, accent=SIGNAL)
b2 = block(s, wrap("Значит, школьнику остаются учебник и тетрадь. Между тем ежедневная короткая практика "
                   "по 10–15 минут — именно то, что даёт язык, и именно то, что удобно делать с телефона.", TEXT, 12.5, rw - 0.56),
           rx + 0.28, yy + 0.30, rw - 0.56, TEXT, 12.5, SOFT, leading=18)
panel_behind(s, rx, yy, rw, bottom=b2, pad=0.30)
footnote(s, "Источник: duolingo.com/courses, раздел «Курсы для тех, кто знает русский». Проверено 25.08.2026.")
notes(s, """
Казахский обязателен в школе. Но тренажёра, где объяснение идёт по-русски,
практически нет.

Важно: я проверил список курсов Duolingo на их собственном сайте в день подготовки
доклада и указал дату. Если жюри спросит — можно открыть сайт прямо сейчас.

Не говорить «Duolingo плохой». Говорить: он рассчитан на языки с большой аудиторией,
и это нормально. Ниша казахского остаётся незакрытой.
""")

# ═══════════════════════════════════════════════════ 03 · вопрос и гипотеза
s, y = sl("проблема исследования", dark=True)
y = title(s, "Существующая программа была. Она не работала — и я хотел понять почему", y, dark=True, max_lines=2, start=36)
y = lede(s, "У меня уже была написанная ранее версия тренажёра. Ученики бросали её через несколько заданий. "
            "Вместо того чтобы переписывать наугад, я поставил вопрос как исследовательский.", y + 0.15, dark=True, w=CW * 0.86)
qy = y + 0.50
qx, qw = col(0, 7)
b = block(s, ["Вопрос исследования"], qx + 0.30, qy + 0.30, qw - 0.60, MONO_SB, 8.5, SIGNAL_L, leading=11, tracking=0.16, caps=True)
b = block(s, wrap("Что именно мешает русскоязычному школьнику заниматься казахским "
                  "в цифровом тренажёре — содержание, интерфейс или что-то ещё?", DISPLAY_B, 21, qw - 0.60),
          qx + 0.30, b + 0.16, qw - 0.60, DISPLAY_B, 21, PAPER_ON_D, leading=28, tracking=-0.018)
panel_behind(s, qx, qy, qw, bottom=b, pad=0.30, dark=True, fill=SIGNAL, border=False)
hx, hw = col(7, 5)
b2 = block(s, ["Гипотеза"], hx + 0.30, qy + 0.30, hw - 0.60, MONO_SB, 8.5, SIGNAL_L, leading=11, tracking=0.16, caps=True)
b2 = block(s, wrap("Главное препятствие — не сложность языка, а технические барьеры: ученик не может "
                   "физически ввести правильный ответ, а объяснения написаны не про тот язык.", TEXT_SB, 13, hw - 0.60),
           hx + 0.30, b2 + 0.16, hw - 0.60, TEXT_SB, 13, PAPER_ON_D, leading=19)
panel_behind(s, hx, qy, hw, bottom=b2, pad=0.30, dark=True)
notes(s, """
Ключевая мысль слайда: я не стал сразу переделывать программу. Сначала —
вопрос и гипотеза, потом проверка.

Гипотеза сформулирована так, чтобы её можно было опровергнуть: если бы барьеры
оказались не техническими, гипотеза бы не подтвердилась.
""")

# ═══════════════════════════════════════════════════ 04 · цель и задачи
s, y = sl("цель и задачи")
y = title(s, "Цель: превратить нерабочий прототип в тренажёр, которым можно пользоваться каждый день", y, max_lines=2, start=34)
ty = y + 0.42
tasks = [
    ("01", "Провести аудит", ["Разобрать существующую программу: код, данные, интерфейс",
                              "Зафиксировать каждый найденный дефект с доказательством"]),
    ("02", "Устранить барьеры", ["Переработать проверку ответов и типы заданий",
                                 "Исправить учебное содержание"]),
    ("03", "Довести до устройства", ["Собрать мобильное приложение",
                                     "Обеспечить работу без интернета"]),
    ("04", "Проверить на учениках", ["Спланировать апробацию",
                                     "Встроить в приложение сбор данных"]),
]
_w = col(0, 3)[1]
CH = row_h(_w, [(t_, h, i) for t_, h, i in tasks], head_size=17, item_size=10.5)
for i, (num, head, items) in enumerate(tasks):
    x, w = col(i * 3, 3)
    yy = block(s, [num], x + 0.28, ty + 0.28, w - 0.56, MONO_SB, 9, SIGNAL, leading=11, tracking=0.16)
    hs, hl = fit(head, DISPLAY_B, w - 0.56, 2, 17, 13, 0.5, -0.018)
    yy = block(s, hl, x + 0.28, yy + 0.14, w - 0.56, DISPLAY_B, hs, INK, leading=hs * 1.14, tracking=-0.018)
    bullets(s, items, x + 0.28, yy + 0.18, w - 0.56, size=10.5, gap=0.12, mcolor=SIGNAL)
    panel_behind(s, x, ty, w, CH, accent=SIGNAL if i == 0 else None)
band(s, ty + CH + 0.28, [("Объект исследования — ", TEXT_SB, WHITE),
     ("процесс самостоятельного изучения казахской грамматики русскоязычным школьником. ", TEXT, WHITE),
     ("Предмет — ", TEXT_SB, WHITE),
     ("технические и содержательные барьеры, которые этот процесс останавливают.", TEXT, WHITE)], size=12)
notes(s, """
Задачи перечислять быстро, по одному предложению на каждую. Жюри читает само.

Если спросят про объект и предмет — они внизу слайда, прочитать дословно.
""")

# ═══════════════════════════════════════════════════ 05 · методика
s, y = sl("методика исследования")
y = title(s, "Как я искал причины", y, max_lines=1, start=38)
y = lede(s, "Я применил три метода, каждый даёт проверяемый результат, а не мнение.", y + 0.15)
my = y + 0.46
methods = [
    ("метод 1", "Сплошной анализ данных", PETROL,
     ["Разобрал все 187 заданий программой, а не глазами",
      "Посчитал, в скольких ответах есть буквы вне русской раскладки",
      "Результат — число, которое можно перепроверить"]),
    ("метод 2", "Проверка шрифтов по таблице глифов", SIGNAL,
     ["Скачал файлы шрифтов и прочитал их таблицы символов",
      "Проверил наличие девяти казахских букв в каждом",
      "Результат — список отсутствующих глифов"]),
    ("метод 3", "Экспертная сверка содержания", OCHRE,
     ["Сверил каждое правило с грамматикой казахского языка",
      "Каждое исправление записал с обоснованием",
      "Результат — журнал из 46 задокументированных правок"]),
]
_b = []
for i, (tag, head, cl, items) in enumerate(methods):
    x, w = col(i * 4, 4)
    yy = block(s, [tag], x + 0.28, my + 0.28, w - 0.56, MONO_SB, 8.5, cl, leading=10.5, tracking=0.16, caps=True)
    hs, hl = fit(head, DISPLAY_B, w - 0.56, 2, 19, 14, 0.5, -0.018)
    yy = block(s, hl, x + 0.28, yy + 0.14, w - 0.56, DISPLAY_B, hs, INK, leading=hs * 1.14, tracking=-0.018)
    _b.append(bullets(s, items, x + 0.28, yy + 0.20, w - 0.56, size=10.5, gap=0.12, mcolor=cl))
_PH = max(_b) + 0.28 - my
for i, (tag, head, cl, items) in enumerate(methods):
    x, w = col(i * 4, 4)
    panel_behind(s, x, my, w, _PH, accent=cl)
footnote(s, "Все три метода воспроизводимы: скрипты анализа лежат в репозитории проекта и запускаются одной командой.")
notes(s, """
Это слайд, который отличает научный проект от поделки. Подчеркнуть:
я не «посмотрел и решил», а измерил.

Ключевая фраза: «любой может запустить мои скрипты и получить те же числа».
""")

# ═══════════════════════════════════════════════════ 06 · находка 1
s, y = sl("результат · находка 1", dark=True)
y = title(s, "90% заданий требуют букв, которых нет на клавиатуре ученика", y, dark=True, max_lines=2, start=37)
y = lede(s, "В казахском алфавите девять букв, которых нет в русском: ә ғ қ ң ө ұ ү һ і. "
            "На школьном компьютере и на телефоне стоит русская раскладка — набрать их негде.", y + 0.15, dark=True, w=CW * 0.86)
ky = y + 0.52
kpi_row(s, [("168", "из 187 заданий"), ("90%", "всех ответов"), ("9", "букв вне раскладки"), ("0", "способов их ввести")],
        ky, dark=True, size=40, label_size=8.5, gap=0.30)
by = ky + 1.34
hline(s, ML, by, CW, HAIR_D)
lx, lw = col(0, 6)
rx, rw = col(6, 6)
block(s, wrap("Старая проверка сравнивала строки точно. Ученик писал «окып жатырмын» — "
              "грамматически верно, просто без казахских букв — и получал «неверно».", TEXT_SB, 13, lw),
      lx, by + 0.28, lw, TEXT_SB, 13, PAPER_ON_D, leading=19)
block(s, wrap("Ученик делает вывод, что не понимает язык, хотя на самом деле он не может "
              "дотянуться до буквы. Это и есть та точка, где занятия бросают.", TEXT, 12.5, rw),
      rx, by + 0.28, rw, TEXT, 12.5, SOFT_D, leading=18)
notes(s, """
Это главный слайд доклада. Здесь нужно сделать паузу.

Хорошо работает живая демонстрация: показать на телефоне русскую клавиатуру
и попросить кого-нибудь из жюри найти букву «қ».

Число 168 из 187 получено скриптом, не на глаз.
""")

# ═══════════════════════════════════════════════════ 07 · находка 2
s, y = sl("результат · находка 2")
y = title(s, "Шрифт приложения не содержал казахских букв", y, max_lines=1, start=37)
y = lede(s, "Я скачал файлы шрифтов, которые использовала программа, и прочитал их таблицы символов. "
            "Оказалось, что часть букв в них просто отсутствует — и браузер молча подставлял чужой шрифт "
            "прямо в учебном тексте.", y + 0.15, w=CW * 0.88)
ty = y + 0.50
rows = [
    ["Manrope (основной текст)", "ә ғ қ ң ұ", "5 из 9 отсутствуют"],
    ["Playfair Display (заголовки)", "ә ғ қ ң ө ү һ", "7 из 9 отсутствуют"],
    ["Golos Text (выбран взамен)", "—", "покрытие полное"],
]
table(s, [("шрифт", 0.42), ("каких букв нет", 0.30), ("итог", 0.28)], rows, ML, ty, CW,
      row_size=12, row_h=0.52, mono_cols=(1,), accent_col=2)
by = ty + 0.52 * 4 + 0.30
band(s, by, [("Ученик видел казахские буквы в другом начертании и не мог этого объяснить. ", TEXT_SB, WHITE),
             ("Дефект был невидим для разработчика: на экране текст отображался, просто разными шрифтами. "
              "Найти его можно было только чтением таблицы глифов.", TEXT, WHITE)], size=12.5)
footnote(s, "Проверка: библиотека fontTools, чтение таблицы cmap каждого файла шрифта. Скрипт в репозитории проекта.")
notes(s, """
Здесь жюри обычно оживляется: дефект неочевидный.

Объяснить простыми словами: в файле шрифта есть список букв, которые он умеет
рисовать. Если буквы в списке нет, браузер берёт её из другого шрифта.
Текст читается, но выглядит как склейка из двух почерков.

Если спросят, как нашёл: прочитал таблицу cmap — это оглавление шрифта.
""")

# ═══════════════════════════════════════════════════ 08 · находка 3
s, y = sl("результат · находка 3")
y = title(s, "Русские объяснения описывали русскую грамматику, а не казахскую", y, max_lines=2, start=36)
y = lede(s, "В программе рядом с казахским текстом стоял русский. Я предполагал, что это перевод. "
            "При сплошной сверке выяснилось, что это отдельный курс — про другой язык.", y + 0.15, w=CW * 0.88)
cy = y + 0.50
lx, lw = col(0, 6)
rx, rw = col(6, 6)
b = block(s, ["было"], lx + 0.30, cy + 0.28, lw - 0.60, MONO_SB, 8.5, ERROR, leading=11, tracking=0.16, caps=True)
b = block(s, wrap("«Настоящее длительное время (Present Continuous) в русском языке образуется "
                  "с помощью действительного причастия на -щий/-ащий…»", TEXT, 12, lw - 0.60),
          lx + 0.30, b + 0.16, lw - 0.60, TEXT, 12, INK, leading=18)
b = block(s, wrap("Правило про русский язык, названное английским термином. "
                  "В русском языке такого времени нет вовсе.", TEXT_MD, 11, lw - 0.60),
          lx + 0.30, b + 0.18, lw - 0.60, TEXT_MD, 11, SOFT, leading=16)
panel_behind(s, lx, cy, lw, bottom=b, pad=0.30, accent=ERROR)
b2 = block(s, ["стало"], rx + 0.30, cy + 0.28, rw - 0.60, MONO_SB, 8.5, PETROL, leading=11, tracking=0.16, caps=True)
b2 = block(s, wrap("«Настоящее время процесса (нақ осы шақ): основа глагола + -ып/-іп/-п + "
                   "вспомогательный глагол жатыр, отыр, тұр или жүр.»", TEXT, 12, rw - 0.60),
           rx + 0.30, b2 + 0.16, rw - 0.60, TEXT, 12, INK, leading=18)
b2 = block(s, wrap("Правило про казахский язык, с примером и разбором. "
                   "Переписано около 151 тысячи знаков в уроках, правилах и справочнике.", TEXT_MD, 11, rw - 0.60),
           rx + 0.30, b2 + 0.18, rw - 0.60, TEXT_MD, 11, SOFT, leading=16)
panel_behind(s, rx, cy, rw, bottom=b2, pad=0.30, accent=PETROL)
notes(s, """
Показать контраст «было — стало», прочитать вслух левую колонку.
Обычно этого достаточно, чтобы стало смешно и понятно одновременно.

Объём: 151 тысяча знаков — это примерно три школьных сочинения в день
в течение месяца. Такой ориентир помогает почувствовать масштаб.
""")

# ═══════════════════════════════════════════════════ 09 · находка 4
s, y = sl("результат · находка 4", dark=True)
y = title(s, "И 46 ошибок в самом учебном материале", y, dark=True, max_lines=1, start=38)
y = lede(s, "Сверяя правила с грамматикой казахского языка, я нашёл ошибки, которые учили неправильному. "
            "Каждую исправил и записал обоснование — чтобы правку мог проверить учитель.", y + 0.15, dark=True, w=CW * 0.86)
ly = y + 0.50
items = [
    ("Страдательный залог назывался притяжательным падежом", "ырықсыз етіс, а не тәуелдік септік"),
    ("Побудительный залог назывался «сезуші етіс»", "правильно — өзгелік етіс"),
    ("Утверждалось, что -дай/-дей образует превосходную степень", "этот аффикс выражает уподобление"),
    ("Утверждалось, что после числительного нужно множественное число", "в казахском — «бес кітап», единственное"),
    ("Утверждалось, что в казахском есть приставки", "их нет: все аффиксы стоят после корня"),
    ("Несуществующие формы в ответах: «қайталанып», «әзірдеп», «мектепхана»", "заменены на реальные слова языка"),
]
yy = ly
for i, (wrong, right) in enumerate(items):
    hline(s, ML, yy, CW, HAIR_D)
    lx, lw = col(0, 7)
    rx, rw = col(7, 5)
    block(s, wrap(wrong, TEXT_SB, 11.5, lw), lx, yy + 0.14, lw, TEXT_SB, 11.5, PAPER_ON_D, leading=16)
    block(s, wrap(right, TEXT, 11, rw), rx, yy + 0.14, rw, TEXT, 11, SIGNAL_L, leading=16)
    yy += 0.62
hline(s, ML, yy, CW, HAIR_D)
footnote(s, "Полный журнал исправлений с обоснованием каждого — файл tools/corrections.json в репозитории проекта.", dark=True)
notes(s, """
Не зачитывать все шесть. Выбрать два самых понятных — про приставки
и про «бес кітап».

Важная мысль для жюри: я не стал молча править учебный материал.
Каждое изменение записано вместе с обоснованием, чтобы учитель мог
проверить и не согласиться.
""")

# ═══════════════════════════════════════════════════ 10 · решение 1
s, y = sl("решение · проверка ответов")
y = title(s, "Проверка ответов теперь понимает ученика", y, max_lines=1, start=38)
y = lede(s, "Вместо точного сравнения строк — четыре слоя проверки. Ответ засчитывается, "
            "если верна грамматика, а не набор символов.", y + 0.15)
sy = y + 0.46
layers = [
    ("слой 1", "Точное совпадение", "Ответ полностью совпал с эталоном"),
    ("слой 2", "Свёртка казахских букв", "«окып» и «оқып» считаются одним ответом; ученику показывается верное написание"),
    ("слой 3", "Допуск опечаток", "Расстояние Левенштейна: одна-две описки прощаются, короткие слова — нет"),
    ("слой 4", "Несколько верных вариантов", "Там, где язык допускает два ответа, принимаются оба"),
]
_w = col(0, 3)[1]
_bb = []
for i, (tag, head, desc) in enumerate(layers):
    x, w = col(i * 3, 3)
    yy = block(s, [tag], x + 0.26, sy + 0.26, w - 0.52, MONO_SB, 8.5, SIGNAL, leading=10.5, tracking=0.16, caps=True)
    hs, hl = fit(head, DISPLAY_B, w - 0.52, 2, 16, 12.5, 0.5, -0.018)
    yy = block(s, hl, x + 0.26, yy + 0.12, w - 0.52, DISPLAY_B, hs, INK, leading=hs * 1.14, tracking=-0.018)
    _bb.append(block(s, wrap(desc, TEXT, 10.5, w - 0.52), x + 0.26, yy + 0.14, w - 0.52, TEXT, 10.5, SOFT, leading=15))
_PH = max(_bb) + 0.26 - sy
for i in range(4):
    x, w = col(i * 3, 3)
    panel_behind(s, x, sy, w, _PH)
by = sy + _PH + 0.28
band(s, by, [("Плюс экранный ряд казахских букв. ", TEXT_SB, WHITE),
             ("Девять кнопок под полем ввода: ә ғ қ ң ө ұ ү һ і. Ученик может набрать правильный ответ "
              "даже на школьном компьютере с русской раскладкой.", TEXT, WHITE)], size=12.5)
footnote(s, "Движок проверки покрыт 16 автотестами: они проверяют и то, что верное засчитывается, и то, что неверное — нет.")
notes(s, """
Здесь важно подчеркнуть баланс: проверка стала мягче к опечаткам,
но не стала мягче к грамматике.

Если спросят «а не будет ли она засчитывать неправильное» — ответ:
короткие слова опечаток не прощают вовсе, «тұр» и «тұт» не сольются,
и на это есть отдельный тест.
""")

# ═══════════════════════════════════════════════════ 11 · решение 2
s, y = sl("решение · типы заданий")
y = title(s, "Одно упражнение превратилось в семь", y, max_lines=1, start=38)
y = lede(s, "В прежних данных ни у одного из 187 шагов не было указано тип задания, поэтому все они "
            "работали как «впиши текст», а код остальных типов не выполнялся никогда.", y + 0.15, w=CW * 0.88)
hy = y + 0.50
hbars(s, [
    ("Заполни пропуск", 52, "52"),
    ("Выбор верной формы", 44, "44"),
    ("Собери предложение", 29, "29"),
    ("Развёрнутый ответ", 23, "23"),
    ("Свободный ввод", 15, "15"),
    ("Сопоставление", 13, "13"),
    ("Перевод на казахский", 11, "11"),
], ML, hy, CW * 0.62, bar_h=0.30, gap=0.20, label_w=CW * 0.24,
   colors=[SIGNAL, SIGNAL, PETROL, OCHRE, PAPER_3, PETROL, OCHRE], size=11)
rx, rw = col(8, 4)
b = block(s, wrap("Варианты ответа генерируются по правилам казахской морфологии", TEXT_SB, 12.5, rw - 0.56),
          rx + 0.28, hy + 0.28, rw - 0.56, TEXT_SB, 12.5, INK, leading=18)
b = bullets(s, ["Смена лица: ішемін / ішесің / ішеміз / ішесіз",
                "Смена времени: келді / келеді / келер",
                "Другие формы того же глагола: талқылап / талқылады / талқылау",
                "Формы с невозможными сочетаниями звуков отбраковываются"],
            rx + 0.28, b + 0.18, rw - 0.56, size=10.5, gap=0.12, mcolor=SIGNAL)
panel_behind(s, rx, hy, rw, bottom=b, pad=0.28, accent=SIGNAL)
footnote(s, "Задания порождаются скриптом из исходного содержания и проверяются автоматически: "
            "в наборе не может оказаться задания без верного ответа или с двумя одинаковыми вариантами.")
notes(s, """
Главное здесь: варианты ответа не случайные. Неверный вариант — это
реальная форма того же глагола, только в другом лице или времени.
Именно такие ошибки ученик и делает в жизни.

Пример на слайде можно прочитать вслух: ішемін — ішесің — ішеміз.
""")

# ═══════════════════════════════════════════════════ 12 · решение 3
s, y = sl("решение · повторение и дизайн", dark=True)
y = title(s, "Приложение возвращает задание ровно тогда, когда его пора вспомнить", y, dark=True, max_lines=2, start=36)
ty = y + 0.44
lx, lw = col(0, 6)
rx, rw = col(6, 6)
b = block(s, ["интервальное повторение"], lx + 0.30, ty + 0.28, lw - 0.60, MONO_SB, 8.5, SIGNAL_L, leading=11, tracking=0.16, caps=True)
b = block(s, wrap("Каждое задание — карточка со своим сроком. Ответил уверенно — вернётся через "
                  "несколько дней. Ошибся — вернётся завтра. Интервалы растут: 1 → 3 → дальше по "
                  "коэффициенту, который зависит от того, как ученик отвечал раньше.", TEXT, 12, lw - 0.60),
          lx + 0.30, b + 0.16, lw - 0.60, TEXT, 12, PAPER_ON_D, leading=18)
b = block(s, wrap("Прежний экран повторения показывал пройденное подряд, без учёта забывания.", TEXT_MD, 11, lw - 0.60),
          lx + 0.30, b + 0.16, lw - 0.60, TEXT_MD, 11, SOFT_D, leading=16)
panel_behind(s, lx, ty, lw, bottom=b, pad=0.30, dark=True, accent=SIGNAL_L)
b2 = block(s, ["интерфейс"], rx + 0.30, ty + 0.28, rw - 0.60, MONO_SB, 8.5, PETROL_L, leading=11, tracking=0.16, caps=True)
b2 = bullets(s, ["Шрифт с полным казахским покрытием, вшит в приложение",
                 "Светлая и тёмная темы, контраст проверен по стандарту WCAG",
                 "Убраны эффекты, мешавшие читать: размытия, градиентный фон, частицы",
                 "Разбор ответа подсвечивает окончание — то, чему и учит урок"],
                rx + 0.30, b2 + 0.16, rw - 0.60, dark=True, size=11.5, gap=0.13, mcolor=PETROL_L)
panel_behind(s, rx, ty, rw, bottom=b2, pad=0.30, dark=True)
footnote(s, "Контраст палитры проверяется скриптом: 28 проверок, все проходят стандарт WCAG AA.", dark=True)
notes(s, """
Про интервальное повторение сказать одним предложением: это известный
метод, я применил его упрощённый вариант — алгоритм SM-2.

Если спросят, откуда метод: он описан в работах по психологии памяти,
кривая забывания Эббингауза. Я не изобретал его, а применил.
""")

# ═══════════════════════════════════════════════════ 13 · демонстрация
s, y = sl("демонстрация")
y = title(s, "Как это выглядит и работает", y, max_lines=1, start=38)
y = lede(s, "Живой показ: открыть урок, ответить русскими буквами и показать, что ответ засчитан "
            "с подсказкой о правильном написании.", y + 0.15)
dy = y + 0.46
steps_demo = [
    ("01", "Открыть урок", "Диалог с персонажем, затем правило, затем задание"),
    ("02", "Ответить без казахских букв", "Написать «окып жатырмын» — так, как может ученик"),
    ("03", "Показать результат", "«Грамматика верна» плюс подсказка: в казахском пишется қ"),
    ("04", "Открыть повторение", "Задание с ошибкой уже стоит в очереди на завтра"),
]
_bd = []
for i, (num, head, desc) in enumerate(steps_demo):
    x, w = col(i * 3, 3)
    yy = block(s, [num], x + 0.26, dy + 0.26, w - 0.52, MONO_SB, 9, SIGNAL, leading=11, tracking=0.16)
    hs, hl = fit(head, DISPLAY_B, w - 0.52, 2, 16, 12, 0.5, -0.018)
    yy = block(s, hl, x + 0.26, yy + 0.12, w - 0.52, DISPLAY_B, hs, INK, leading=hs * 1.14, tracking=-0.018)
    _bd.append(block(s, wrap(desc, TEXT, 10.5, w - 0.52), x + 0.26, yy + 0.14, w - 0.52, TEXT, 10.5, SOFT, leading=15))
_PH = max(_bd) + 0.26 - dy
for i in range(4):
    x, w = col(i * 3, 3)
    panel_behind(s, x, dy, w, _PH)
by = dy + _PH + 0.30
band(s, by, [("Запасной вариант. ", TEXT_SB, WHITE),
             ("Если в аудитории нет интернета — приложение установлено на телефон заранее "
              "и работает офлайн. Показ не зависит от связи.", TEXT, WHITE)], size=12.5)
notes(s, """
ЖИВАЯ ДЕМОНСТРАЦИЯ. Отрепетировать до автоматизма, уложиться в 60–90 секунд.

Обязательно проверить накануне:
— приложение установлено на телефон и открывается;
— заряд телефона;
— если показ через проектор — светлая тема включена, она читается лучше.

Не показывать больше четырёх шагов: жюри устаёт.
""")

# ═══════════════════════════════════════════════════ 14 · мобильное приложение
s, y = sl("продукт · мобильное приложение")
y = title(s, "Приложение ставится на телефон и работает без интернета", y, max_lines=2, start=37)
y = lede(s, "Тренажёр нужен там, где им пользуются: в дороге, дома, на перемене. "
            "Поэтому всё содержание лежит внутри приложения, а не запрашивается с сервера.", y + 0.16, w=CW * 0.88)
ay = y + 0.48
ways = [
    ("Android · APK", SIGNAL, ["Файл устанавливается напрямую, магазин не нужен",
                               "Собирается автоматически при каждом изменении",
                               "Скачивается по ссылке или QR-коду"]),
    ("iPhone и любой браузер · PWA", PETROL, ["Открыть сайт и выбрать «На экран „Домой“»",
                                              "Появляется иконка, запускается без адресной строки",
                                              "После первого запуска работает офлайн"]),
    ("Компьютер · сайт", OCHRE, ["Работает в любом браузере без установки",
                                 "Подходит для урока в компьютерном классе",
                                 "Один и тот же прогресс сохраняется на устройстве"]),
]
_ba = []
for i, (head, cl, items) in enumerate(ways):
    x, w = col(i * 4, 4)
    hs, hl = fit(head, DISPLAY_B, w - 0.56, 2, 19, 14, 0.5, -0.018)
    yy = block(s, hl, x + 0.28, ay + 0.28, w - 0.56, DISPLAY_B, hs, INK, leading=hs * 1.14, tracking=-0.018)
    _ba.append(bullets(s, items, x + 0.28, yy + 0.20, w - 0.56, size=10.5, gap=0.12, mcolor=cl))
_PH = max(_ba) + 0.28 - ay
for i, (head, cl, items) in enumerate(ways):
    x, w = col(i * 4, 4)
    panel_behind(s, x, ay, w, _PH, accent=cl)
footnote(s, "Размер приложения: около 1,3 МБ вместе со шрифтами, изображениями и всем учебным содержанием. "
            "Установка занимает секунды даже на медленном интернете.")
notes(s, """
Если жюри спросит, почему не в Google Play: публикация требует платного
аккаунта разработчика и проверки в несколько дней. Для школьного проекта
установка по ссылке решает ту же задачу, а сборка полностью автоматическая.

Про офлайн сказать обязательно — в сельской школе это решающий довод.
""")

# ═══════════════════════════════════════════════════ 15 · апробация
s, y = sl("апробация · план эксперимента", dark=True)
y = title(s, "Как я буду проверять, что стало лучше", y, dark=True, max_lines=1, start=38)
y = lede(s, "Приложение само записывает, как ученик отвечал, и выгружает данные в таблицу. "
            "Это даёт объективные числа вместо впечатлений.", y + 0.15, dark=True, w=CW * 0.86)
ty = y + 0.48
timeline(s, [
    ("01", "Предварительный срез", "день 1", ["Контрольная работа на бумаге", "Анкета: сколько минут в день на казахский"]),
    ("02", "Занятия", "дни 2–6", ["По 10–15 минут в день в приложении", "Данные пишутся автоматически"]),
    ("03", "Итоговый срез", "день 7", ["Такая же работа, другие слова", "Сравнение с первым срезом"]),
    ("04", "Обработка", "дни 8–10", ["Выгрузка CSV, расчёт показателей", "Сравнение с контрольной группой"]),
], ML, ty, CW, dark=True)
by = ty + 1.90
hline(s, ML, by, CW, HAIR_D)
lx, lw = col(0, 6)
rx, rw = col(6, 6)
block(s, wrap("Что измеряется: доля верных ответов, время на задание, число возвратов "
              "к одному заданию, доля ответов, набранных без казахских букв.", TEXT_SB, 12, lw),
      lx, by + 0.26, lw, TEXT_SB, 12, PAPER_ON_D, leading=17)
block(s, wrap("Персональные данные не собираются. В таблице стоит только метка участника "
              "вида «7А-12», которую задаёт сам ученик.", TEXT, 11.5, rw),
      rx, by + 0.26, rw, TEXT, 11.5, SOFT_D, leading=17)
notes(s, """
ЧЕСТНО СКАЗАТЬ: на момент защиты апробация ещё не проведена — это план
эксперимента, а не результаты.

Если апробация к защите пройдена, заменить этот слайд на реальные числа:
средний балл до и после, точность по типам заданий, отзывы.

Если жюри спросит «а есть результаты?» — ответ прямой: «Пока нет.
Инструмент сбора данных встроен, протокол готов, эксперимент занимает
семь учебных дней». Это лучше, чем придуманные проценты.
""")

# ═══════════════════════════════════════════════════ 16 · как я это делал
s, y = sl("как это сделано")
y = title(s, "Из чего собран проект", y, max_lines=1, start=38)
y = lede(s, "Приложение написано на тех же инструментах, на которых делают настоящие продукты. "
            "Ниже — что за что отвечает.", y + 0.15)
ty = y + 0.46
rows = [
    ["TypeScript", "Язык программирования", "Проверяет ошибки до запуска: нельзя случайно сложить число со строкой"],
    ["React", "Библиотека интерфейса", "Экран описывается как функция от данных — меньше ручной работы с разметкой"],
    ["Vite", "Сборщик", "Собирает исходники в файлы, которые открывает браузер"],
    ["Capacitor", "Упаковка в приложение", "Оборачивает готовый сайт в настоящее Android-приложение"],
    ["GitHub Actions", "Автоматическая сборка", "После каждого изменения сам собирает APK в облаке"],
    ["Python", "Скрипты обработки данных", "Генерация заданий, проверка контраста, аудит шрифтов"],
]
table(s, [("инструмент", 0.20), ("что это", 0.24), ("зачем в проекте", 0.56)], rows, ML, ty, CW,
      row_size=11, row_h=0.44, mono_cols=(0,))
by = ty + 0.44 * 7 + 0.24
band(s, by, [("Объём работы: ", TEXT_SB, WHITE),
             ("около 4 700 строк собственного кода, 31 автоматический тест, "
              "46 задокументированных исправлений учебного материала и примерно 151 тысяча знаков "
              "переписанного содержания.", TEXT, WHITE)], size=12.5)
notes(s, """
Этот слайд — для вопроса «сам ли ты это писал».

Отвечать спокойно и конкретно: рассказать, что делает каждый инструмент,
и показать любой файл в коде на выбор жюри. Тот, кто разбирался в проекте,
объяснит устройство; тот, кто не разбирался, — нет.

Хорошо иметь наготове ответ, что именно было труднее всего.
У меня это правила казахской морфологии для генерации вариантов ответа.
""")

# ═══════════════════════════════════════════════════ 17 · выводы
s, y = sl("выводы", dark=True)
y = title(s, "Что показало исследование", y, dark=True, max_lines=1, start=40)
cy = y + 0.46
concl = [
    ("01", "Гипотеза подтвердилась",
     "Главные барьеры оказались техническими, а не языковыми: невозможность ввести ответ, "
     "шрифт без нужных букв, объяснения не про тот язык."),
    ("02", "Барьеры устранимы",
     "Все четыре найденные причины удалось убрать, не меняя учебную программу: "
     "изменились проверка, содержание и интерфейс, а темы остались школьные."),
    ("03", "Метод важнее результата",
     "Дефекты нашлись потому, что их искали измерением, а не на глаз. Тот же подход "
     "применим к любому учебному приложению."),
]
yy = cy
for num, head, desc in concl:
    hline(s, ML, yy, CW, HAIR_D)
    nx, nw = col(0, 1)
    hx, hw = col(1, 4)
    dx, dw = col(5, 7)
    block(s, [num], nx, yy + 0.20, nw, MONO_SB, 11, SIGNAL_L, leading=13, tracking=0.14)
    hs, hl = fit(head, DISPLAY_B, hw, 2, 20, 15, 0.5, -0.018)
    block(s, hl, hx, yy + 0.18, hw, DISPLAY_B, hs, PAPER_ON_D, leading=hs * 1.14, tracking=-0.018)
    block(s, wrap(desc, TEXT, 11.5, dw), dx, yy + 0.20, dw, TEXT, 11.5, SOFT_D, leading=17)
    yy += 1.12
hline(s, ML, yy, CW, HAIR_D)
notes(s, """
Выводы читать медленно. Это последнее, что запомнит жюри.

Третий вывод — самый ценный с научной точки зрения: метод переносится
на другие приложения. Если будет вопрос «в чём новизна», отвечать отсюда.
""")

# ═══════════════════════════════════════════════════ 18 · дальше
N[0] += 1
s = d.add(dark=True)
rect(s, 0, 0, W, H, fill=INK)
signal_field(s, ML, 0.90, CW, 1.00, n=132, seed=5,
             base=RGBColor(0x2B, 0x31, 0x39), hi=SIGNAL_L, hi_from=0.40, hi_to=0.56)
hline(s, ML, 2.06, CW, RGBColor(0x2B, 0x31, 0x39))
block(s, ["что дальше"], ML, 2.26, CW, MONO_SB, 9, SIGNAL_L, leading=11.5, tracking=0.20, caps=True)
yy = block(s, wrap("Провести апробацию и открыть приложение школам", DISPLAY_XB, 42, CW * 0.86),
           ML, 2.72, CW, DISPLAY_XB, 42, PAPER_ON_D, leading=48, tracking=-0.026)
lx, lw = col(0, 4)
mx, mw = col(4, 4)
rx, rw = col(8, 4)
plans = [
    (lx, lw, "ближайшее", ["Провести эксперимент на одном классе", "Заменить план эксперимента реальными числами"]),
    (mx, mw, "следующий шаг", ["Записать озвучку с носителем языка", "Добавить уроки по школьной программе"]),
    (rx, rw, "перспектива", ["Режим учителя: статистика класса", "Открыть исходный код для других школ"]),
]
for x, w, tag, items in plans:
    b = block(s, [tag], x, yy + 0.50, w, MONO_SB, 8.5, SIGNAL_L, leading=10.5, tracking=0.16, caps=True)
    bullets(s, items, x, b + 0.14, w, dark=True, size=11, gap=0.12, mcolor=SIGNAL_L)
block(s, [f"{AUTHOR} · {GRADE} · {SCHOOL}"], ML, H - MB - 0.52, CW, TEXT_MD, 11,
      RGBColor(0x8B, 0x92, 0x9B), leading=15)
block(s, [f"{SITE} · спасибо за внимание"], ML, H - MB - 0.14, CW, MONO_MD, 8.5,
      RGBColor(0x6B, 0x72, 0x7B), leading=11, tracking=0.10, caps=True)
notes(s, """
Финал. Сказать одно предложение и остановиться — дальше вопросы.

Готовые ответы на частые вопросы:
— «Чем лучше Duolingo?» → Там нет казахского для русскоязычных, я проверял на их сайте.
— «Сам писал?» → Показать код, объяснить любой файл на выбор.
— «Есть результаты на учениках?» → Пока нет, эксперимент спланирован, занимает 7 дней.
— «Кто проверял грамматику?» → Каждое исправление записано с обоснованием, готов показать журнал.
— «Сколько стоит?» → Ничего: работает офлайн, сервер не нужен.
""")

d.save("GrammarQuest-защита.pptx")
if OVERFLOW:
    print(f"\n✗ переполнений в карточках: {len(OVERFLOW)}")
    for o in OVERFLOW:
        print("  " + o)
else:
    print("\n✓ переполнений в карточках нет")
print(f"Слайдов: {N[0]}")
